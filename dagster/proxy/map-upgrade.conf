# "Connection: upgrade" seulement quand le client le demande, "close" sinon.
# Poser la valeur en dur casserait les requetes HTTP ordinaires.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
