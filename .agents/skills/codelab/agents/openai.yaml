interface:
  display_name: "Codelab"
  short_description: "Repo-specific patterns and workflows for codelab"
  default_prompt: "Use the codelab repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true